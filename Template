#include<bits/stdc++.h>
#pragma GCC optimize("Ofast")

using str = std::string;
using ull = unsigned long long;
using ii = std::pair <int, int>;

#define X first
#define Y second
#define endl '\n'
#define int long long
#define Xd << '\n' <<
#define And << " " <<
#define Dd(x) (long long)x.length()
#define End cout Xd "\n"; return 0;
#define Time_start clock_t start = clock();
#define reset(a, x) memset(a, x, sizeof(a));
#define Fu(i, l, r) for (int i = l; i <= r; i++)
#define Nhapmang(x, n) Fu(i, 1, n) cin >> x[i];
#define Inmang(x, n) Fu(i, 1, n) cout << x[i] << " ";
#define File(x) freopen(x".INP", "r", stdin); freopen(x".OUT", "w", stdout);
#define Fast ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
#define Time_end(start) clock_t end=clock();cout Xd(double)(end-start)/CLOCKS_PER_SEC<<'s';
using namespace std;

const int N = 1e2 + 5;

/*-------------// Variables \\--------------*/
    
/*------------------------------------------*/

void solve()
{
    
}

int32_t main()
{
    Fast;
    // freopen("test.inp", "r", stdin);
    // int t; cin >> t; while(t--)
        solve();
    End;
}
